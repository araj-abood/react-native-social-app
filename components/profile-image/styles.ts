import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  profileImageBorder: {
    borderRadius: 1000,
    padding: 2,
    borderWidth: 1,
    borderColor: '#F35BAC',
  },
});

export default styles;
